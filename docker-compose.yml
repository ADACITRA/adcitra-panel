version: '3.8'

services:
  adcitra-panel:
    build: .
    image: adcitra/panel:1.0.0
    container_name: adcitra-panel
    restart: unless-stopped
    ports:
      - \"8888:8888\"
    volumes:
      - adcitra_data:/app/data
    environment:
      - TZ=Asia/Shanghai

volumes:
  adcitra_data:
